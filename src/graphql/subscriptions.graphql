subscription subscribeToChannels{
    channelMessages {
        id
        channel {
          id
        }
        character {
          id
          name
        }
        message
        timestamp
    }
}
